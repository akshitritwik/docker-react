sudo: required
services:
  - docker

before_install:
  - docker build -t akshitritwik/docker-react -f Dockerfile.dev .

script:
  -  docker run -e CI=true akshitritwik/docker-react npm run test -- -- coverage